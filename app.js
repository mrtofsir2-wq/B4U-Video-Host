const video=document.getElementById('video');const status=document.getElementById('status');video.addEventListener('loadedmetadata',()=>{status.textContent=`Ready • ${Math.round(video.duration)}s`});video.addEventListener('play',()=>status.textContent='Playing');video.addEventListener('pause',()=>status.textContent='Paused');video.addEventListener('ended',()=>status.textContent='Finished');video.addEventListener('error',()=>status.textContent='Video could not be loaded');
